program SveZivo

const int konstanta = 10;
int niz[];
int globalni_i;

{
    bool verify(int x) {
        print('v');
        print(x);
        return x > 5;
    }

    void main() 
        int i, j;
        int rez;
    {
        niz = new int[5];
        i = 0;
        
        // Test 1: For petlja + Nizovi + Switch
        for (i = 0; i < 5; i++) {
            niz[i] = i;
            
            print(i);
            switch (i) {
                case 1:
                    print('A');
                    break;
                case 2:
                case 3:
                    print('B');
                    if (i == 3) continue; // Preskače eol i ispis 'C'
                    print('X'); // Ovo se ispisuje samo za i=2
                    break;
            }
            print(eol); 
        }

        print(eol);
        
        // Test 2: Short-circuit + Kompleksni uslovi
        // Ako je niz[0] == 0 (true), verify(1) se NE POKREĆE (||)
        // Ako je niz[1] == 1 (true), verify(2) SE POKREĆE (&&)
        if (niz[0] == 0 || verify(1)) {
            print('S'); // Success 1
        }
        
        if (niz[1] == 1 && verify(9)) {
            print('S'); // Success 2 (ispisaće v9 pre S)
        } else {
            print('F');
        }

        print(eol);

        // Test 3: Ugnježdene petlje, Break i Continue
        i = 0;
        for (i = 0; i < 3; i++) {
            for (j = 0; j < 3; j++) {
                if (i == j) continue;   // Preskače ispis istih brojeva
                if (i == 2) break;      // Prekida unutrašnju petlju kad je i=2
                
                print(i);
                print(j);
                print(' ');
            }
            // Ovaj eol se ispisuje za svaku iteraciju spoljne petlje
            print(eol);
        }
        
        print('K'); // Kraj
               
                
        
    }
}